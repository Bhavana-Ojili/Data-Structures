n=int(input())
arr=list(map(int,input().split()))
arr.sort()
if len(arr)==n+1:
    for i in range(len(arr)):
        if arr.count(i)==2:
            print("there is duplicate")
        else:
            print("no duplicate")
